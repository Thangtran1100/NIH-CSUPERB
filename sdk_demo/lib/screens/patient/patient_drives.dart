import 'package:flutter/material.dart';

class DrivesPage extends StatelessWidget {
  const DrivesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    
      // Rest of your Drives page content
    );
  }
}
